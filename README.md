# memory

Cross-platform memory allocation, deallocation, copy, and string length operations

## Exports

```alloc, free, memcpy, strlen, str_from_int, str_concat```n
## Install

```otter pkg add memory 1.0.0
otter pkg pull```n
